{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "SamplingResult",
  "description": "Schema for the Distributed Training Engine SamplingResult envelope",
  "type": "object",
  "required": [
    "datasetId",
    "samplePath",
    "sampleCount"
  ],
  "properties": {
    "datasetId": {
      "type": "string",
      "minLength": 1,
      "description": "Unique identifier of the sampled dataset"
    },
    "samplePath": {
      "type": "string",
      "minLength": 1,
      "description": "Path to the representative sample artifact file (<dataset_id>_sample.pt)"
    },
    "sampleCount": {
      "type": "integer",
      "minimum": 1,
      "description": "Count of samples extracted (default: 1)"
    }
  },
  "additionalProperties": false
}
